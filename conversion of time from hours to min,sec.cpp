#include<stdio.h>
int main()
{
	int h,m,s;
	printf("enter the value of hour:");
	scanf("%d",&h);
	h=m*60;
	s=m*60;
	pintf("\n the value of time in minutes is: %d",+m);
	printf("\n the value of time in seconds is: %d",+s);
}
